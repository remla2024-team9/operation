apiVersion: apps/v1
kind: Deployment
metadata:
  name: model-service
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: model-service
  template:
    metadata:
      labels:
        app: model-service
    spec:
      containers:
        - name: model-service
          image: ghcr.io/remla2024-team9/model-service:latest
          ports:
            - containerPort: 5000
